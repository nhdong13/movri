.row
  .col-12
    .listing-header
      = link_to "Home", ""
      %span / Camera
      %p Camera
  .col-12
    .brown-bar
      %span Sort by:
      = select_tag "sort_listing", options_for_select(sort_conditions, selected: params[:sort_condition])

  - listings.each do |listing|
    .col-3
      .listing-box
        .main-image
          = image_tag listing.main_image, :class => "design-image-too-wide"
        .listing-price
          %span= MoneyViewUtils.to_humanized(Money.new(listing.price_cents, 'USD'))
          %span /1 day
        .listing-information
          = listing.title
        = link_to "Rent now", listing_path(listing), class: "rent-now-btn"