%script{:async => "", :src => "//static.klaviyo.com/onsite/js/klaviyo.js?company_id=HLsMny", :type => "text/javascript"}
:javascript
  var _learnq = _learnq || [];
  _learnq.push(["track", "Fulfilled Order", JSON.parse('#{ raw @fulfill_data.to_json }')]);
  document.addEventListener("DOMContentLoaded", () => document.getElementById("edit_transaction_#{@order.id}").submit());
%body
  = form_for [:admin, @order], url: admin_community_transaction_path(@current_community, @order) do |f|
    = hidden_field_tag :direct_page, @redirect_path
    = f.hidden_field :fulfilled_at, value: Time.current
