_learnq.push(['identify', { '$email' : '<%= @current_user ? @current_user.get_email : "un_login_user@gmail.com" %>'}]);

_learnq.push(["track", "Refunded Order",
  <%= raw @refunded_data.to_json %>
]);

window.location.href = '<%= @redirect_path%>'