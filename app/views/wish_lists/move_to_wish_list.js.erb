<% if @can_add_to_wish_list %>
  swal("Successfully!", '<%= @message %>', "success", {buttons: false, timer: 1000}).then(function() {location.reload();});
<% else %>
  swal("Failure!", '<%= @message %>', "error");
<% end %>